# FastBite WPF Desktop Skill

Primary AI-agent skill for this desktop workspace.

Use `SKILL.md` when working on WPF UI, MVVM, theme resources, reusable controls, navigation shell, desktop API integration, or XAML refactoring.

The older `gpt-taste` skill name is retained as a compatibility shim, but its content now points to WPF desktop rules instead of web/React/GSAP guidance.

